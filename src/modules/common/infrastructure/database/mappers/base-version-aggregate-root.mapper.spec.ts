import { BaseVersionAggregateRootMapper } from './base-version-aggregate-root.mapper';

describe('BaseVersionAggregateRootMapper', () => {
  it('should be defined', () => {
    expect(new BaseVersionAggregateRootMapper()).toBeDefined();
  });
});
