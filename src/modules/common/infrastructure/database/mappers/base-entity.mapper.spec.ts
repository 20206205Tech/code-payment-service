import { BaseEntityMapper } from '../../../../chat/infrastructure/database/mappers/base-entity.mapper';

describe('BaseEntityMapper', () => {
  it('should be defined', () => {
    expect(BaseEntityMapper).toBeDefined();
  });
});
