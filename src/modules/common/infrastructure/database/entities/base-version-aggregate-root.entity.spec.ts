import { BaseVersionAggregateRootEntity } from './base-version-aggregate-root.entity';

describe('BaseVersionAggregateRootEntity', () => {
  it('should be defined', () => {
    expect(new BaseVersionAggregateRootEntity()).toBeDefined();
  });
});
