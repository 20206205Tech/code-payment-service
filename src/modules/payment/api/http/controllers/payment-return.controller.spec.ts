import { PaymentReturnController } from './payment-return.controller';

describe('PaymentReturnController', () => {
  it('should be defined', () => {
    expect(new PaymentReturnController()).toBeDefined();
  });
});
