import { ArchivePlanController } from './archive-plan.controller';

describe('ArchivePlanController', () => {
  it('should be defined', () => {
    expect(new ArchivePlanController()).toBeDefined();
  });
});
