import { GetMySubscriptionController } from './get-my-subscription.controller';

describe('GetMySubscriptionController', () => {
  it('should be defined', () => {
    expect(new GetMySubscriptionController()).toBeDefined();
  });
});
