import { GetAllPlanController } from './get-all-plan.controller';

describe('GetAllPlanController', () => {
  it('should be defined', () => {
    expect(new GetAllPlanController()).toBeDefined();
  });
});
