import { ManualActivateTransactionCommand } from './manual-activate-transaction.command';

describe('ManualActivateTransactionCommand', () => {
  it('should be defined', () => {
    expect(new ManualActivateTransactionCommand()).toBeDefined();
  });
});
