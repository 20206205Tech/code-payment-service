import { PaymentCallbackCommand } from './payment-callback.command';

describe('PaymentCallbackCommand', () => {
  it('should be defined', () => {
    expect(new PaymentCallbackCommand()).toBeDefined();
  });
});
