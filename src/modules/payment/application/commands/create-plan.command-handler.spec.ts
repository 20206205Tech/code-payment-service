import { CreatePlanCommandHandler } from './create-plan.command-handler';

describe('CreatePlanCommandHandler', () => {
  it('should be defined', () => {
    expect(new CreatePlanCommandHandler()).toBeDefined();
  });
});
