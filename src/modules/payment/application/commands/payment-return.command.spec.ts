import { PaymentReturnCommand } from './payment-return.command';

describe('PaymentReturnCommand', () => {
  it('should be defined', () => {
    expect(new PaymentReturnCommand()).toBeDefined();
  });
});
