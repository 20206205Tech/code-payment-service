import { PaymentCallbackCommandHandler } from './payment-callback.command-handler';

describe('PaymentCallbackCommandHandler', () => {
  it('should be defined', () => {
    expect(new PaymentCallbackCommandHandler()).toBeDefined();
  });
});
