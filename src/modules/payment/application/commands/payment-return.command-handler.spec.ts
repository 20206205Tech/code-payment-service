import { PaymentReturnCommandHandler } from './payment-return.command-handler';

describe('PaymentReturnCommandHandler', () => {
  it('should be defined', () => {
    expect(new PaymentReturnCommandHandler()).toBeDefined();
  });
});
