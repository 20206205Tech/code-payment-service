import { PaymentGatewayServicePort } from './payment-gateway.service.port';

describe('PaymentGatewayServicePort', () => {
  it('should be defined', () => {
    expect(PaymentGatewayServicePort).toBeDefined();
  });
});
