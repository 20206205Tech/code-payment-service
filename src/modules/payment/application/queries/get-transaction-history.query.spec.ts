import { GetTransactionHistoryQuery } from './get-transaction-history.query';

describe('GetTransactionHistoryQuery', () => {
  it('should be defined', () => {
    expect(new GetTransactionHistoryQuery()).toBeDefined();
  });
});
