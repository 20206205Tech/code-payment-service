import { SepayGatewayService } from './sepay-gateway.service';

describe('SepayGatewayService', () => {
  it('should be defined', () => {
    expect(new SepayGatewayService()).toBeDefined();
  });
});
