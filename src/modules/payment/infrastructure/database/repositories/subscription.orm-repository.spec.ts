import { SubscriptionOrmRepository } from './subscription.orm-repository';

describe('SubscriptionOrmRepository', () => {
  it('should be defined', () => {
    expect(new SubscriptionOrmRepository()).toBeDefined();
  });
});
